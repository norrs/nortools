# JSON Output

Most tools support `--json`.

```bash
nortools mx --json example.com
```

Use JSON when you want to:

- Save evidence during an incident.
- Compare output over time.
- Pipe results into another tool.
- Include structured diagnostics in a support ticket.

Human output is optimized for quick reading. JSON output is optimized for repeatability.
